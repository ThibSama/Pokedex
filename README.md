# Pokédex

Expo / React Native Pokédex app using Expo Router.

## Get started

```bash
npm install
npx expo start
```

## Checks

```bash
npm run lint
npx tsc --noEmit
```

Routes live in `src/app/` (file-based routing). AI usage is traced in `docs/ai-usage.jsonl`.
